	进程：一个其中运行着多个线程的地址空间和线程所需要的系统资源.
	fork():
		写实复制，信号位图被清除. 文件描述符打开, 用户目录, 当前目录+1. 
	exec():
		不返回, 不会关闭原来的文件描述符.
	僵尸进程:
	  	#1: 在子进程结束运行后, 父进程读取其状态之前, ---僵尸态
	  	#2: 父进程退出之后(先死了), 子进程死之前(已经挂载到init上) ---僵尸态

	wait wait(pid)(在父进程中):及时获取子进程的返回信息, 并释放内核资源.
		 ./test_0.cpp  ./test_1.cpp
	
