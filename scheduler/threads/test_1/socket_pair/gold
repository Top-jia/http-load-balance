对于socketpair的进行测试:
	1:当创建了一个双端管道, 
			如: test_0.c 对其写入数据, 只能从另一端来进行读取, test_1.c.
		不能从一端写入, 在从其读出,(将会阻塞),
	2:两端同时写入, 只从一端读取, 再从另一端读出.
			如图 socketpair.png
			test_2.c
	3:对于在多进程中使用socketpair来进行操作, 
			可以对sockpair的描述符进行设置, 非阻塞模式,
		同理, 也可以将其注册事件机制, epoll的事件机制,
		可以同时处理读写事件.
